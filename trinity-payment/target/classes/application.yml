server:
  port: 8096

spring:
  application:
    name: trinity-payment
  config:
    import: nacos:trinity-payment.yaml
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: dev
      config:
        server-addr: 127.0.0.1:8848
        namespace: dev
        file-extension: yaml
        refresh-enabled: true

logging:
  level:
    com.trinity: DEBUG
